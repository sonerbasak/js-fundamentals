// if you have more than one element with the same id, it will return the first one
const clearButton = document.querySelector('#clearButton');

console.log(clearButton);

const li = document.querySelectorAll('input:nth-child(1)');

console.log(li);

